<% subject, *messages = commit.message.lines %>

<%= subject.strip %>
===

<%= messages.join %>

<% parent, _, _ = adjacent_commits(commit) %>

<% (parent.present? ? parent.diff(commit) : commit.diff(commit)).each do |diff| %>
<%= diff.path %>
---

```diff
<%= diff.patch %>
```
<% end %>
